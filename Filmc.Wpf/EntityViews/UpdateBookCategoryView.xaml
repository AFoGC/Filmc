<UserControl x:Class="Filmc.Wpf.EntityViews.UpdateBookCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Filmc.Wpf.EntityViews"
             xmlns:menus="clr-namespace:Filmc.Wpf.Views"
             xmlns:con="clr-namespace:Filmc.Wpf.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <local:BindingProxy x:Key="categoryProxy" Data="{Binding}"/>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Margin="20,10">
            <StackPanel.Resources>
                <Style TargetType="TextBox" BasedOn="{StaticResource Wtextbox}">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                    <Setter Property="Height" Value="20"/>
                </Style>
                <Style TargetType="ComboBox" BasedOn="{StaticResource DarkComboBox}">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                    <Setter Property="Height" Value="20"/>
                </Style>
                <Style TargetType="Button" BasedOn="{StaticResource GrayButton}">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                    <Setter Property="Height" Value="20"/>
                </Style>
                <Style TargetType="CheckBox">
                    <Setter Property="Margin" Value="0,0,0,10"/>
                    <Setter Property="Height" Value="20"/>
                </Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="WhiteSmoke"/>
                    <Setter Property="FontSize" Value="10"/>
                </Style>
            </StackPanel.Resources>

            <TextBlock Text="{DynamicResource id_tt}" />
            <TextBox Text="{Binding Id}" ToolTip="{DynamicResource int_utt}" IsReadOnly="True" Width="50" HorizontalAlignment="Left" />

            <TextBlock Text="{DynamicResource name_tt}" />
            <TextBox Text="{Binding Name}" ToolTip="{DynamicResource string_utt}"/>

            <TextBlock Text="{DynamicResource hidename_tt}" />
            <TextBox Text="{Binding HideName}" ToolTip="{DynamicResource string_utt}"/>

            <TextBlock Text="{DynamicResource mark_tt}" />
            <StackPanel Orientation="Horizontal">
                <ComboBox SelectedItem="{Binding FormatedMark, Mode=TwoWay}" ToolTip="{DynamicResource cb_utt}" Width="145"
                          HorizontalAlignment="Left"
                          ItemsSource="{Binding Path=DataContext.BookMarks, 
                          RelativeSource={RelativeSource AncestorType={x:Type menus:UpdateMenuView}}, Mode=Default}">

                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0}/{1}">
                                        <Binding Path="" />
                                        <Binding RelativeSource="{RelativeSource AncestorType={x:Type menus:UpdateMenuView}}" Path="DataContext.FilmMarkSystem"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <Rectangle Width="10"/>
                <Button Content="{DynamicResource remove_mark_btn}" Command="{Binding RemoveMarkCommand}" Padding="10,0" VerticalAlignment="Top" />
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding BooksVC.View}">
            <DataGrid.Style>
                <Style TargetType="DataGrid" BasedOn="{StaticResource MainGrid}">
                    <Setter Property="CellStyle">
                        <Setter.Value>
                            <Style TargetType="DataGridCell" BasedOn="{StaticResource MainGridCell}">
                                <Setter Property="ContextMenu">
                                    <Setter.Value>
                                        <ContextMenu>
                                            <MenuItem Header="{DynamicResource edit_btn}" ToolTip="{DynamicResource open_edit_tt}"
                                                      Command="{Binding OpenUpdateMenuCommand}"
                                                      CommandParameter="{Binding}"/>

                                            <MenuItem Header="{Binding SourcesText}" ToolTip="{DynamicResource copy_url_tt}" 
                                                      Command="{Binding CopyUrlCommand}"/>

                                            <Separator />
                                            <MenuItem Header="{DynamicResource up_in_category_cm}"
                                                      Command="{Binding Path=Data.UpInCategoryCommand, Source={StaticResource categoryProxy}}"
                                                      CommandParameter="{Binding}"/>

                                            <MenuItem Header="{DynamicResource down_in_category_cm}"
                                                      Command="{Binding Path=Data.DownInCategoryCommand, Source={StaticResource categoryProxy}}"
                                                      CommandParameter="{Binding}"/>

                                            <MenuItem Header="{DynamicResource remove_from_category_cm}"
                                                      Command="{Binding Path=Data.RemoveFromCategoryCommand, Source={StaticResource categoryProxy}}"
                                                      CommandParameter="{Binding}"/>
                                        </ContextMenu>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Style>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Жанр" Binding="{Binding Genre.Name}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Год" Binding="{Binding PublicationYear}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Прочтение" Binding="{Binding ReadProgress.Name}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Закладка" Binding="{Binding Bookmark}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Дата начала чтения" Binding="{Binding StartReadDate, StringFormat=dd.MM.yyyy}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Дата конца чтения" Binding="{Binding FullReadDate, StringFormat=dd.MM.yyyy}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Оценка" Width="80" IsReadOnly="True">
                    <DataGridTextColumn.Binding>
                        <MultiBinding>
                            <MultiBinding.Converter>
                                <con:MarkValueConverter />
                            </MultiBinding.Converter>
                            <Binding Path="FormatedMark"/>
                            <Binding Path="MarkSystem"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Прочтения" Binding="{Binding CountOfReadings}" Width="100" IsReadOnly="True"/>

                <!-- Tags -->
                <DataGridTemplateColumn Header="Теги" HeaderStyle="{StaticResource TagsColumnHeader}" CellStyle="{StaticResource TagsGridCell}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding BookTags.SortView}" Style="{StaticResource TagsItemsControl}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
